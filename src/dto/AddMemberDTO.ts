export interface AddMemberDTO {
    userId: string; // ID del usuario que será el miembro
    projectId: string; // ID del proyecto al que pertenece el miembro
    roleId: string; // ID del rol del miembro
  }
  